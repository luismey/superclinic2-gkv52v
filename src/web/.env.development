# Application Environment
# Identifies development environment for configuration and feature flags
NEXT_PUBLIC_APP_ENV=development

# API Configuration
# Base URL and version for backend API endpoints
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_API_VERSION=v1

# Firebase Configuration v9.0+
# Authentication and cloud services configuration for development
NEXT_PUBLIC_FIREBASE_API_KEY=${FIREBASE_API_KEY}
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=${FIREBASE_AUTH_DOMAIN}
NEXT_PUBLIC_FIREBASE_PROJECT_ID=${FIREBASE_PROJECT_ID}
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=${FIREBASE_STORAGE_BUCKET}
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=${FIREBASE_MESSAGING_SENDER_ID}
NEXT_PUBLIC_FIREBASE_APP_ID=${FIREBASE_APP_ID}

# WebSocket Configuration
# Real-time communication endpoint for chat and notifications
NEXT_PUBLIC_WEBSOCKET_URL=ws://localhost:8000/ws

# Monitoring and Analytics (Optional)
# Development environment tracking and error monitoring
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=${GOOGLE_ANALYTICS_ID}
NEXT_PUBLIC_SENTRY_DSN=${SENTRY_DSN}

# Note: All NEXT_PUBLIC_ variables are exposed to the browser
# Ensure sensitive values are properly managed in deployment pipeline
# Reference: src/web/.env.example for required variable formats